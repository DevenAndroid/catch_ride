import 'package:flutter/material.dart';

class AvailabilityBodyworkScreen extends StatelessWidget {
  const AvailabilityBodyworkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bodywork Availability')),
      body: const Center(child: Text('Bodywork Availability/Rates go here')),
    );
  }
}
