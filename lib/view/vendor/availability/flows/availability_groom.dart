import 'package:flutter/material.dart';

class AvailabilityGroomScreen extends StatelessWidget {
  const AvailabilityGroomScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Groom Availability')),
      body: const Center(child: Text('Groom Availability/Rates go here')),
    );
  }
}
