import 'package:flutter/material.dart';

class AvailabilityClippingScreen extends StatelessWidget {
  const AvailabilityClippingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Clipping Availability')),
      body: const Center(child: Text('Clipping Availability/Rates go here')),
    );
  }
}
