import 'package:flutter/material.dart';

class AvailabilityBraidingScreen extends StatelessWidget {
  const AvailabilityBraidingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Braiding Availability')),
      body: const Center(child: Text('Braiding Availability/Rates go here')),
    );
  }
}
