import 'package:flutter/material.dart';

class AvailabilityFarrierScreen extends StatelessWidget {
  const AvailabilityFarrierScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Farrier Availability')),
      body: const Center(child: Text('Farrier Availability/Rates go here')),
    );
  }
}
